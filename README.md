# HC Parekh Backend

Node.js/Express backend for the HC Parekh Admin Dashboard and Website.

## Technologies
- Node.js
- Express
- MongoDB
- Mongoose
- CORS
- Dotenv

## Setup
1. Clone the repository
2. Run `npm install`
3. Create a `.env` file with `MONGO_URI` and `PORT`
4. Run `npm run dev`
